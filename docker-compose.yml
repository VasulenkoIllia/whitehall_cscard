services:

  db:
    image: postgres:16-alpine
    container_name: whitehall-db
    environment:
      POSTGRES_USER: whitehall_store
      POSTGRES_PASSWORD: whitehall_store
      POSTGRES_DB: whitehall_store
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    ports:
      # Доступ з локального ПК через SSH-тунель (DataGrip):
      #   ssh -L 5433:localhost:5432 user@whitehallshop.workflo.space
      # Потім в DataGrip: host=localhost, port=5433
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U whitehall_store -d whitehall_store"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - app-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whitehall-app
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://whitehall_store:whitehall_store@db:5432/whitehall_store
      PORT: 3000
      TZ: Europe/Kyiv
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    # Міграції запускаються автоматично при кожному старті (ідемпотентні)
    command: sh -c "node dist/scripts/runMigrations.js && node dist/index.js"
    restart: unless-stopped
    networks:
      - app-net
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.whitehall.rule=Host(`whitehallshop.workflo.space`)"
      - "traefik.http.routers.whitehall.entrypoints=websecure"
      - "traefik.http.routers.whitehall.tls.certresolver=cf"
      - "traefik.http.services.whitehall.loadbalancer.server.port=3000"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"require('http').get('http://127.0.0.1:3000/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1));\""
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  app-net:
    driver: bridge
  proxy:
    external: true
